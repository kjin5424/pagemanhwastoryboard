// 1️⃣ ?. (옵셔널 체이닝)
// 의미: "있으면 접근하고, 없으면 undefined 반환"
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● 예시 1: 기본 상황
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const episode = {
  pages: {
    "page-0001": { memo: "첫 페이지" },
  },
};

// ✅ episode가 있으면 pages에 접근
const page = episode?.pages["page-0001"];
console.log(page); // { memo: "첫 페이지" }

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● 예시 2: episode가 없을 때
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const episode = null;

// ❌ 옵셔널 체이닝 안 쓰면 에러 발생
const page = episode.pages["page-0001"];
// ❌ Error: Cannot read property 'pages' of null

// ✅ 옵셔널 체이닝 쓰면 안전
const page = episode?.pages["page-0001"];
console.log(page); // undefined (에러 안 남!)

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 비교: 옵셔널 체이닝 유무
// ❌ 옵셔널 체이닝 없이 안전하게 쓰려면 이렇게 해야 함
let page;
if (episode && episode.pages) {
  page = episode.pages[pageId];
} else {
  page = undefined;
}

// ✅ 옵셔널 체이닝 쓰면 한 줄로 끝
const page = episode?.pages[pageId];

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● 옵셔널 체이닝 활용
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const user = {
  profile: {
    address: {
      city: "서울",
    },
  },
};

// ✅ 안전하게 깊은 곳까지 접근
const city = user?.profile?.address?.city; // "서울"

// ❌ 중간에 없으면 에러
const city = user.profile.address.city; // 만약 profile이 없으면 에러!

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● NOT 연산자 활용
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const items = [];

if (!items.length) {
  console.log("아이템이 없습니다"); // 실행됨
}

// items.length가 0이므로
// !0 → true
// 따라서 if문 실행

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// !(NOT 연산자)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● 기본 개념
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
!true; // false
!false; // true

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ● "없음"을 체크할 때 사용
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
const value = null;

// 값이 있는지 확인
if (value) {
  console.log("값이 있어요"); // 실행 안 됨
}

// 값이 없는지 확인
if (!value) {
  console.log("값이 없어요"); // 실행됨!
}

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 다음 값들은 모두 "없음"으로 취급됨
!null; // true
!undefined; // true
!0; // true
!""; // true (빈 문자열)
!false; // true

// 반대로, 값이 있으면 "있음"으로 취급
!"hello"; // false (값이 있으므로)
!123; // false (값이 있으므로)
!true; // false
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
